    <%- include('../partials/head', { title: title || 'Verificación Pendiente - Algorithmics' }) %>
    <link rel="stylesheet" href="/styles/pages/dashboards/dashboard.css">
    <style>
        .verification-container {
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            background: linear-gradient(135deg, var(--primary-500) 0%, var(--primary-200) 100%);
            padding: 2rem;
        }

        .verification-card {
            background: var(--bg-primary);
            border-radius: 1.5rem;
            padding: 3rem;
            max-width: 600px;
            width: 100%;
            box-shadow: var(--shadow-xl);
            text-align: center;
        }

        .verification-icon {
            width: 120px;
            height: 120px;
            margin: 0 auto 2rem;
            background: var(--warning-100);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            animation: pulse 2s infinite;
        }

        .verification-icon i {
            font-size: 3rem;
            color: var(--warning-500);
        }

        @keyframes pulse {
            0%, 100% {
                transform: scale(1);
                opacity: 1;
            }
            50% {
                transform: scale(1.05);
                opacity: 0.8;
            }
        }

        .verification-title {
            font-size: 2rem;
            color: var(--primary-700);
            margin-bottom: 1rem;
            font-weight: 700;
        }

        .verification-message {
            font-size: 1.125rem;
            color: var(--text-secondary);
            margin-bottom: 2rem;
            line-height: 1.6;
        }

        .verification-info {
            background: var(--bg-secondary);
            border-radius: 0.75rem;
            padding: 1.5rem;
            margin-bottom: 2rem;
            text-align: left;
        }

        .verification-info h3 {
            color: var(--primary-600);
            margin-bottom: 1rem;
            font-size: 1.25rem;
        }

        .verification-info ul {
            list-style: none;
            padding: 0;
        }

        .verification-info li {
            padding: 0.5rem 0;
            color: var(--text-secondary);
            display: flex;
            align-items: center;
            gap: 0.75rem;
        }

        .verification-info li i {
            color: var(--success-500);
            font-size: 1.25rem;
        }

        .user-info {
            background: var(--primary-50);
            border-radius: 0.75rem;
            padding: 1rem;
            margin-bottom: 2rem;
        }

        .user-info p {
            margin: 0.5rem 0;
            color: var(--text-secondary);
        }

        .user-info strong {
            color: var(--primary-700);
        }

        .logout-btn {
            margin-top: 1rem;
        }
    </style>
</head>

<body>
    <div class="verification-container">
        <div class="verification-card">
            <div class="verification-icon">
                <i class="fas fa-clock"></i>
            </div>

            <h1 class="verification-title">Cuenta en Verificación</h1>
            
            <p class="verification-message">
                ¡Gracias por registrarte en Algorithmics! Tu cuenta ha sido creada exitosamente 
                y está pendiente de verificación por parte de nuestro equipo administrativo.
            </p>

            <div class="user-info">
                <p><strong>Nombre:</strong> <%= usuario.nombre %> <%= usuario.apellido %></p>
                <p><strong>Email:</strong> <%= usuario.email %></p>
                <p><strong>Estado:</strong> <span style="color: var(--warning-600);">Pendiente de verificación</span></p>
            </div>

            <div class="verification-info">
                <h3>¿Qué sigue?</h3>
                <ul>
                    <li>
                        <i class="fas fa-check-circle"></i>
                        <span>Un administrador revisará tu solicitud</span>
                    </li>
                    <li>
                        <i class="fas fa-check-circle"></i>
                        <span>Recibirás un correo cuando tu cuenta sea activada</span>
                    </li>
                    <li>
                        <i class="fas fa-check-circle"></i>
                        <span>Podrás acceder a todos los recursos de la plataforma</span>
                    </li>
                </ul>
            </div>

            <div class="verification-info">
                <h3>Tiempo estimado</h3>
                <p style="color: var(--text-secondary); margin: 0;">
                    <i class="fas fa-info-circle" style="color: var(--primary-500);"></i>
                    La verificación suele completarse en un plazo de 24-48 horas hábiles.
                </p>
            </div>

            <button onclick="logout()" class="btn btn-secondary logout-btn">
                <i class="fas fa-sign-out-alt" style="margin-right: 0.5rem;"></i> Cerrar Sesión
            </button>
        </div>
    </div>

    <script>
        function logout() {
            // Eliminar cookie de token
            document.cookie = 'token=; expires=Thu, 01 Jan 1970 00:00:00 UTC; path=/;';
            window.location.href = '/';
        }
    </script>
</body>
</html>
